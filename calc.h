#ifndef CALC_H
#define CALC_H

#include <cmath>

// Declare the functions used in calc.cpp
double calculate_case1(double x, int n);
double calculate_case2(double x, int n);
double y(double x, int n);
void input_data(double& a, double& b, int& n, double& step);

#endif // CALC_H